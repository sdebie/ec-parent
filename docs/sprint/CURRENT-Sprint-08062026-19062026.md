# Task List Sprint-08062026-19-062026

## Admin Portal

### Brands

- [v] – Add toggle to be able to switch between either uploading a new image or selecting an image from the library.
- [] - Nice to Add import/export functionality for brands. This should be a CSV file with columns: name, slug,
  description, logo_url. The import should handle creating new brands and updating existing ones based on the slug.

### Categories

- [v] – Add toggle to be able to switch between either uploading a new image or selecting an image from the library.
- [] – Add import/export functionality for categories. This should be a CSV file with columns: name, slug, description,
  logo_url, parent_category_slug (for nested categories). The import should handle creating new categories and updating
  existing ones based on the slug.

### Products

- [v] – Start with of create functionality of a new product.

### Settings

- [v] – Move the config from the TypeScript files to the backend.
- [v] – Enusre nginx gets config from the backend and inject into the frontend, so the frontend does not need to call
  the backend to get the config.
- [] - Add a dropdown to select the currency. The dropdown should be populated with a list of common currencies (e.g.,
  USD, EUR, GBP) and their symbols. When a currency is selected, it should update the store_settings table with the
  selected currency code and symbol. The currency symbol should be displayed in the currency box component.

#### Imports for Image Gallery

- [] - Add a toggle button to display images or list of images. List of images should make it easier to bulk select to
  delete images, making it easier for managing purposes. NB: Ensure before deleting the image is not used in any other
  table like products.

### Order Management

- [] - Add a workflow for updating order status (e.g., pending → processing → shipped → delivered).
- [] - Add the ability to view order details, including customer information, items ordered, and payment status.
- [] - Send email notification to the customer when order status changes.
- [] - Business account for the courier guy because you get an invoice/account for all the deliveries that occurred in
  that month.

### Wholesale Management

- [] - Add the ability to accept/reject wholesale requests.
- [] - Add an audit trail for accepting and rejections wholesale applications.

## Storefront

- [] - customer account needs to have extra options to view past orders, manager account/addressorder history /
  tracking (nice to have a customer portal for managing orders)
- [] - change the functionality of the wholesale request form to call api to create customer and email uvh admin.
- [] - create favourite for products for customers who are logged in.
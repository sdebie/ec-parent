# Task List Sprint-08062026-19-062026

## Admin Portal

### Top Menu

- [v] Add a view account for a logged-in user, with the ability to update their name, email, and password.
- [] - ~~Fix the user menu for customers who are logged in.~~

### Brands

- [v] – Add toggle to be able to switch between either uploading a new image or selecting an image from the library.
- [] - Add import/export functionality for brands. This should be a CSV file with columns: name, slug,
  description, logo_url. The import should handle creating new brands and updating existing ones based on the slug.

### Categories

- [v] – Add toggle to be able to switch between either uploading a new image or selecting an image from the library.
- [] – Add import/export functionality for categories. This should be a CSV file with columns: name, slug, description,
  logo_url, parent_category_slug (for nested categories). The import should handle creating new categories and updating
  existing ones based on the slug.

### Products

- [v] – ~~Need a product editor and create a screen.~~
- [] - ~~Add a status column on product and product variances. Also update graphQL queries. First status should be draft.~~
- [] - Add functionality to bulk update product status (e.g., select multiple products and mark them as active or
  inactive) ON THE BATCH FILE.

### Settings

- [v] – ~~Move the config from the TypeScript files to the backend.~~
- [v] – ~~Enusre nginx gets config from the backend and injects into the frontend on startup, so the frontend does not
  need to call the backend to get the config every time it starts.~~
- [] - Add a dropdown to select the currency. The dropdown should be populated with a list of countries. When a country
  is selected, it should update the store_settings table with the selected currency code and symbol. The currency symbol
  should be displayed in the currency box component.

#### Imports for Image Gallery

- [] - Add a toggle button to display images or list of images. List of images should make it easier to bulk select to
  delete images, making it easier for managing purposes. NB: Ensure before deleting the image is not used in any other
  table like products.

### Order Management

- [] - Add a workflow for updating order status (e.g., pending → processing → shipped → delivered).
- [] - Double-check the ability to view order details, including customer information, items ordered, and payment
  status.
- [] - Send email notification to the customer when order status changes.
- [] - Business account for the courier guy because you get an invoice/account for all the deliveries that occurred in
  that month.

### Wholesale Management

- [] - ~~Add the ability to accept/reject wholesale requests.~~
- [] - Add an audit trail for accepting and rejections wholesale applications.

## Storefront

- [] - ~~customer account needs to have extra options in menu to view past orders, manager account/address, order
  history / tracking (nice to have a customer portal for managing orders)~~
- [] - ~~Email client with a notification that a wholesale request. And then email customer that the request has been
  accepted or rejected.~~
- [] - ~~create favourite products for customers who are logged in.~~
- [] - ~~add a search bar to the home page.~~
- [] - Fix the user menu for customers who are logged in.

- []- Build react app in github.